#include <iostream>
using namespace std;

int main() {
    string user_name;
    cout << "Please enter your name below:" << endl;
    cin >> user_name;
    if(user_name == Joey || user_name == joey || user_name == Alex || user_name == alex || user_name == Hannah || user_name == hannah) {
        for(;;) {
            cout << "Get fucked you stupid cunt" << endl;
        }
    }
    else {
        cout << "You seem chill enough, carry on." << endl;
    }
    return 0;
}